pandas>=2.1.0
numpy>=1.24.0
scikit-learn>=1.3.0
scipy>=1.11.0
joblib>=1.3.0
tqdm>=4.66.0

# Text normalization
emoji>=2.8.0
regex>=2023.12.25

# HuggingFace (GPU optional; Colab friendly)
torch>=2.1.0
transformers==4.38.2
datasets==2.17.0
# NOTE: transformers.Trainer will import peft if installed. Colab sometimes has a newer peft
# preinstalled, which can break if accelerate is too old (missing clear_device_cache).
# Pin a compatible pair to avoid that runtime crash.
accelerate==0.30.1
peft==0.10.0
sentencepiece>=0.1.99
safetensors>=0.4.0
